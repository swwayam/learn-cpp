#include "iostream"

int main()
{

    // Even or Odd program

    /*

    int num{};

    std::cout << "Enter your number : ";
    std::cin >> num;

    if (num % 2 == 0)
    {
        std::cout << "It's a even number" << std::endl;
    }
    else
    {
        std::cout << "It's a even number" << std::endl;
    }

    */

    // Eligble for treatment

    /*

    int age{};

    std::cout << "Enter your age to check eligblity : ";
    std::cin >> age;
    

    if (age > 20 && age < 40)
    {
        std::cout << "You are eligible for the Treatment" << std::endl;
    }
    else
    {
        std::cout << "You are not eligible for the Treatment" << std::endl;
    }

    */

    return 0;
}